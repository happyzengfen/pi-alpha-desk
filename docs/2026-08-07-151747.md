# 2026-08-07 15:17:47 更新记录

## 更新概要

- 上游仓库：<https://github.com/isWittHere/pi-web-desktop>
- 上游分支：`main`
- 合并前本地提交：`7a598df`
- 上游最新提交：`f4a0e97`
- 合并提交：`3e0b123`
- 合并范围：上游 53 个提交，共变更 55 个文件，新增 5,899 行，删除 808 行。

## 主要更新内容

### 聊天输入与上下文

- 新增输入框手动高度调整及最大化操作。
- 重构模型、Thinking、工具预设等弹出菜单的布局与样式。
- 允许在流式生成期间修改模型、Thinking 级别和工具预设。
- 新增上下文附加菜单，支持将操作系统文件拖入并转换为 `@file` 提及。
- 新增 `@file` 和 `/skill` 提及高亮。
- 历史用户消息进入编辑时，恢复文本及 Base64 图片。
- 修复 IME 输入期间文本框背景透明效果。

### 会话、草稿与工作区

- 新增草稿会话，首次发送后再持久化为正式会话。
- 修复切换会话时草稿丢失、选中状态错误和空草稿残留问题。
- 工作区切换时恢复上次会话上下文，并缓存会话快照以加快重新打开。
- 工作区选择器新增运行中与未读状态指示。
- 保留本地桌面版修复：首次启动、尚未选择 cwd 时仍显示项目选择器。
- 新增通用上下文菜单和文件资源管理器操作。

### 消息显示与 Markdown

- 历史过程组默认折叠，并改善返回运行中会话时的滚动定位。
- 修复流式 Markdown 部分的 React key 冲突。
- 修复列表内及粘连文本中的块级数学公式规范化。
- 简化会话状态栏，主要显示成本和上下文占用。
- 改善右键菜单、工具栏下拉面板、拖放覆盖层和浮动操作按钮的样式及层级。

### SDK 与运行时

- Pi SDK 升级至 `0.84.0`。
- 修复 `PlainTextTheme` 新背景色键的映射。
- API Key 登录请求取消时中止后续登录流程。
- 新增会话缓存、工作区记忆、Skill 块解析和文件提及等底层模块与测试。

## 合并处理

- 合并时仅 `components/AppShell.tsx` 发生内容冲突。
- 冲突解决保留了本地首次启动项目选择器行为，同时接入上游 `onBackgroundTaskDone` 回调。
- 合并前未提交的 `README.md` 和 `docs/` 文档通过 stash 临时保护，合并后已完整恢复。

## 验证结果

- `npm install --ignore-scripts --no-audit --no-fund`：成功；存在 React 19 与 `@emoji-mart/react` 的 peer dependency 警告。
- `node_modules/.bin/tsc --noEmit`：通过。
- 明确选取 `*.test.mjs` 的有效测试集：294/294 通过。
- 完整 `node --test` 会额外误识别 API 的 `test/route.ts` 和 `scripts/tmp-md-test.mjs`；另有一个 macOS `/var` 与 `/private/var` 实路径差异用例失败。
- `npm run lint`：上游新增的 `scripts/electron-diag.js` 和 `scripts/pill-test-main.js` 触发 3 个 CommonJS `require()` 错误，`scripts/tmp-md-test.mjs` 有 1 个未使用变量警告。

## macOS 桌面版构建

- 构建时间：2026-08-07 15:04–15:06 CST
- 构建命令：`npm run electron:build`
- 平台与架构：macOS arm64（Apple Silicon）
- 应用版本：`0.7.16`
- DMG：`release/数字化AI助手-0.7.16-arm64.dmg`
- 未打包应用：`release/mac-arm64/数字化AI助手.app`
- DMG 大小：约 231 MB
- SHA-256：`cb1aef0bf446075b7749b0e6d6b47e7ea8a1ae2f87e52e2b1ce2ddf480a9fa51`
- `hdiutil verify` 完整性校验通过，主程序确认为 arm64 Mach-O 可执行文件。
- 本机未配置 Apple Developer ID，本次应用未进行正式签名和公证；首次启动可能需要在 Finder 中右键选择“打开”。

## 已知警告

- Next.js Turbopack 对部分动态文件路径给出过宽的文件追踪范围警告，本次生产构建仍成功完成。
- Electron Builder 提示项目当前禁用 ASAR，以及 `package.json` 缺少 `author` 字段。
- 打包过程报告了重复依赖引用，但未阻断应用或 DMG 生成。
