# 2026-08-08 23:25:40 上游同步记录

## 更新概要

- 上游仓库：<https://github.com/isWittHere/pi-web-desktop>
- 上游分支：`main`
- 同步前本地提交：`02ce267`
- 上游最新提交：`127be8e`
- 上游版本标签：`v0.8.6-f`（`262d905`）；上游 `main` 另含 17 个标签后提交
- 本地合并提交：`72b5fa5`
- 提交范围：`f4a0e97..127be8e`，共 18 个上游提交
- 上游变更规模：47 个文件，新增 2,169 行，删除 229 行
- 应用版本：`0.7.16` → `0.8.6-f`

## 主要更新内容

### 过程时间线与交互

- 为每个过程步骤提供独立的纵向滚动窗口，避免长工具输出或思考内容撑高整条时间线。
- 调整过程组折叠箭头和显示模式切换器；折叠箭头在悬停或展开时显示。
- 移除已经失效的 `expanded` 属性、Markdown 样式和插件别名。
- 修复 steer/queue 按钮在鼠标下方发生位移的问题。

### Markdown 与文件预览

- 支持在 Markdown 输出中解析并显示本地路径图片，同时保留受控文件访问边界。
- 统一 Markdown 文件预览与聊天消息的渲染方式。
- 修复 Markdown 代码块的横向滚动行为。
- 新增本地图片路径解析测试，覆盖绝对路径、相对路径、Windows 路径和不安全路径。

### 内置主题

- 将内置主题迁移到 `lib/themes/` 注册表，并补齐深浅色配对与解析测试。
- 新增 Gruvbox Light、Solarized、Miku Aqua、Orbital Rose、Scarlet Tether 等深浅色主题。
- 调整默认边框深度和主题设置项，减少旧 CSS 与重复配置。
- 在 `docs/themes/` 中补充部分可分发主题文件。

### Electron 与 macOS

- 新增 macOS Pi 应用图标与模板化托盘图标，并加入图标生成脚本。
- macOS 窗口启用原生交通灯按钮和隐藏标题栏；Windows/Linux 继续使用现有无边框窗口。
- macOS 保留最小原生应用/编辑菜单，使 `Cmd+C`、`Cmd+V`、`Cmd+Q` 等系统快捷键正常工作。
- 保留本地产品名、内置 Skill 安装、动态服务端口选择和 Windows 托盘行为。

### Worktree 与侧边栏

- 修复 Windows 下 Git POSIX 路径与原生路径分隔符不一致导致的 worktree 识别问题。
- 统一 worktree 切换器和工作区选择器的视觉样式。
- 清理不再使用的 worktree 样式和临时验证脚本。

### 依赖与发布流程

- `package.json` 版本更新为 `0.8.6-f`，新增 `remark-frontmatter` 与 `unist-util-visit`。
- Electron 构建命令在构建前自动执行图标生成。
- 发布脚本新增 `package-lock.json` 快照与失败恢复，避免 `npm dedupe`、`npm prune` 和临时安装 Electron 时污染锁文件。
- 合并时保留本地 Windows x64 多目标构建、预打包验证、本地 Pi 快照恢复和 fork 仓库元信息。

## 合并处理

- 新增上游远端：`iswitthere` → `git@github.com:isWittHere/pi-web-desktop.git`。
- `electron/main.js`：合并 macOS 图标/交通灯逻辑与本地内置 Skill、动态端口启动流程。
- `package.json`：合并图标生成命令，同时保留 `pi:sync-local` 和 Windows x64 发布命令。
- `scripts/build-release.mjs`：保留本地多目标构建与验证流程，并将上游 lockfile 恢复放入现有 `finally` 清理路径。
- 未覆盖或删除本地独立提交、README、Langfuse 文档、Windows CI/打包修复及 bundled skills。

## 验证结果

- `npm install --ignore-scripts --no-audit --no-fund`：成功；仍有 React 19 与 `@emoji-mart/react` 的既有 peer dependency 警告。
- `node_modules/.bin/tsc --noEmit`：通过。
- 源码目录内全部 `*.test.mjs`：318/318 通过。
- `node --check electron/main.js`：通过。
- `node --check scripts/build-release.mjs`：通过。
- `git diff --check`：通过。
- 未运行 `next build` 或 Electron 打包，遵守开发期间不污染 `.next/` 的项目约定。

## 已知问题

- 直接运行 `node --test` 会误扫描 `app/api/models-config/test/route.ts`、`scripts/tmp-md-test.mjs` 和旧 `release/` 产物；macOS `/var` 与 `/private/var` 实路径差异也会影响一个临时目录断言。将源码 `*.test.mjs` 作为明确测试集合并使用真实临时目录后全部通过。
- `npm run lint` 仍被既有诊断脚本阻断：`scripts/electron-diag.js` 与 `scripts/pill-test-main.js` 有 3 个 CommonJS `require()` 规则错误，`scripts/tmp-md-test.mjs` 有 1 个未使用变量警告。本次同步未修改这些文件。
- 本地 `main` 尚未推送到 `origin/main`。
